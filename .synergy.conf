section: screens
	hungrig:
	fummf:
end

section: links
	hungrig:
		left = fummf
	fummf:
		right = hungrig
end

section: aliases
	hungrig:
		hungry.tilefile.com
		kippe.local
		kippe.office.iptego.de
		kippe
		ewq.local
	fummf:
		goldie
		raspberrypi.local
		raspberrypi
        hotty
        hotty.local
		hotty.office.iptego.de
		dhcp-10-0-0-238.office.iptego.de
end

section: options
    keystroke(control+super+h) = switchInDirection(left)
    keystroke(control+super+l) = switchInDirection(right)
end
